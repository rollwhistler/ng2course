import { MyCapitalizeDirective } from './my-capitalize.directive';

describe('MyCapitalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyCapitalizeDirective();
    expect(directive).toBeTruthy();
  });
});
